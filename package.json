{
  "name": "compassshop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "live-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Julia Matos",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "live-server": "^1.2.2",
    "prettier": "^3.3.2"
  }
}
